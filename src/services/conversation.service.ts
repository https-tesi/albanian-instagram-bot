export enum ConversationStatus { AI_ACTIVE = "AI_ACTIVE", HUMAN_REQUIRED = "HUMAN_REQUIRED", HUMAN_ACTIVE = "HUMAN_ACTIVE", RESOLVED = "RESOLVED" }
export enum HandoffReason { HUMAN_REQUESTED = "HUMAN_REQUESTED", AI_UNCERTAIN = "AI_UNCERTAIN", ACTION_FAILED = "ACTION_FAILED", COMPLAINT = "COMPLAINT", USER_RATE_LIMIT = "USER_RATE_LIMIT", BUSINESS_BUDGET_LIMIT = "BUSINESS_BUDGET_LIMIT" }
export interface Conversation { instagramUserId: string; status: ConversationStatus; handoffReason?: HandoffReason; handoffAt?: Date; lastCustomerMessageAt?: Date; lastHumanMessageAt?: Date; latestMessage?: string; aiReentryOfferedAt?: Date; createdAt?: Date; updatedAt?: Date; }
export interface ConversationRepository { get(userId: string): Promise<Conversation>; save(conversation: Conversation): Promise<void>; }
export class InMemoryConversationRepository implements ConversationRepository { private readonly records = new Map<string, Conversation>(); async get(instagramUserId: string): Promise<Conversation> { return this.records.get(instagramUserId) ?? { instagramUserId, status: ConversationStatus.AI_ACTIVE }; } async save(conversation: Conversation): Promise<void> { this.records.set(conversation.instagramUserId, { ...conversation }); } }
export class ConversationService { constructor(private readonly repository: ConversationRepository) {} get(instagramUserId: string): Promise<Conversation> { return this.repository.get(instagramUserId); } async markHumanActive(instagramUserId: string, now = new Date()): Promise<void> { const conversation = await this.get(instagramUserId); await this.repository.save({ ...conversation, status: ConversationStatus.HUMAN_ACTIVE, lastHumanMessageAt: now, updatedAt: now }); } async resolveConversation(instagramUserId: string): Promise<void> { const conversation = await this.get(instagramUserId); await this.repository.save({ ...conversation, status: ConversationStatus.RESOLVED, updatedAt: new Date() }); } }
